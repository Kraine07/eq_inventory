[build]
builder = "java-maven"

[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = [
  "mvn clean package -DskipTests"
]

[start]
cmd = "java -jar target/eq-inventory-0.0.1-SNAPSHOT.jar"
